zuul:
  #忽略框架默认的服务路径
  ignoredServices: *
  #是否开启重试功能
  retryable: true
  routes:
    thelper-user:
       path: /user/**
    thelper-grade:
       path: /grade/**

ribbon:
   #当前服务重试次数
   MaxAutoRetries: 2
   #切换相同Server的次数
   MaxAutoRetriesNextServer: 5